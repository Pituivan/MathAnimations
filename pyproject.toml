[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pituivan-math-animations"
version = "0.0.0"
dependencies = [
    "manim==0.20.1",
    "numpy==2.4.6"
]

[tool.setuptools]
packages = ["utils"]